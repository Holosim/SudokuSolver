// GridFormat.cpp — see GridFormat.h.
//
// SCAFFOLD ONLY. The byte-normative layout of docs/RTVM.md 6.2 is
// implemented under its own issue.
//
// TODO(RTVM-400): real 13-line ASCII rendering.

#include "GridFormat.h"

namespace sudoku {

std::string formatGrid(const Grid& grid)
{
    static_cast<void>(grid);
    return std::string{};
}

} // namespace sudoku
